## Review verdict

### Builder commit 5194337: ✅ APPROVED
- Pipeline dedup: GOOD copy survived — surviving `_prune_pipeline_jobs()` reads TTL from `_cfg.get("pipeline", {}).get("job_ttl_s", 3600)` and correctly prunes by completed_at. The degraded argless version was removed.
- infer_fn injection: All 3 sites in `goal_discovery.py` (lines 226, 264, 323) pass `infer_fn=_infer_fn`. `thought_engine.py:528` passes `infer_fn=_gd._infer_fn`. `api.py` calls `goal_discovery.set_infer_fn(mdl.infer)` at startup before discovery thread starts.
- Tests: Real assertions — `test_goal_discovery_maybe_evolve_receives_infer_fn` uses monkeypatch to capture the `infer_fn` arg and asserts `captured["infer_fn"] is sentinel_fn` (not just import+True).

### Builder commit 07223e9: ✅ APPROVED
- RecommendationStore: Safe — uses `CREATE TABLE IF NOT EXISTS recommendation_hits`, no migration footgun, new DB path `~/.kernel-evolving/workspace/evolution.db`.
- Config wiring: Correct key — `config.get("evolution", {}).get("recommendation_boost", 0.05)` at `evolver.py:185-186`.
- Tests: Real assertions — boost path asserts score difference; non-recommended candidates get no boost; run() persistence verified with actual SQLite query on temp DB.

### New commit 20d92a8 fix(memory): strip critic-retry pollution
- Files changed: src/memory.py, tests/test_memory.py
- New tests: 4, all passing
- Full suite: 345 passed, 15 skipped, 3 deselected

### Inline review notes
- None — all 5 verification points passed cleanly.

### Recommendation to Olly
✅ Ready to push + tag v1.19.2 + release
